#!/bin/sh
ROOT=$(readlink -f $(dirname $0)/..)

$ROOT/bin/piggly trace && \
  (ruby -I"$ROOT/lib" `find $ROOT/example/spec/ -name '*_spec.rb'` 2>trace.txt) && \
  $ROOT/bin/piggly report -f trace.txt

$ROOT/bin/piggly untrace
